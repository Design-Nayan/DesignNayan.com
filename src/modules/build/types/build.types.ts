export interface BuildOffering {
  title: string;
  badge: string;
  desc: string;
  features: string[];
  ctaText: string;
  iconName: string;
}

export interface MaterialCategory {
  category: string;
  brands: string[];
  desc: string;
}
